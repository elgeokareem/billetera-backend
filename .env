# Database connection variables (mirrored from Go project expected names)
HOST_DB=localhost
PORT_DB=5432
USER_DB=postgres
PASSWORD_DB=postgres
DATABASE_DB=app_db
SSL_MODE_DB=disable

# Derived convenience URL (manually compose; shell expansion not automatic in dotenv loaders)
# If you change credentials above, update this line accordingly.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/app_db?sslmode=disable
